% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-fast-online-simple-adaptive.R
\name{OnlineAdaptiveModel}
\alias{OnlineAdaptiveModel}
\alias{FastOnlineSimpleAdaptiveModel}
\title{Define the online adaptive LKT model}
\usage{
OnlineAdaptiveModel(
  alpha_recency_start = 0.03,
  alpha_logsuc_start = 0.05,
  alpha_decay_start = 0.01,
  online_mode = "alpha_only",
  beta_alpha_terms = NULL,
  nonlinear_alpha_terms = character(0),
  alpha_start = NULL,
  alpha_lower = -1,
  alpha_upper = 1,
  nonlinear_lower = 1e-05,
  nonlinear_upper = 0.99999,
  maxit = 100,
  factr = 1e+07,
  optim_trace = 0,
  optim_report = 1,
  require_native = FALSE,
  use_gradient = TRUE
)

FastOnlineSimpleAdaptiveModel(
  alpha_recency_start = 0.03,
  alpha_logsuc_start = 0.05,
  alpha_decay_start = 0.01,
  online_mode = "alpha_only",
  beta_alpha_terms = NULL,
  nonlinear_alpha_terms = character(0),
  alpha_start = NULL,
  alpha_lower = -1,
  alpha_upper = 1,
  nonlinear_lower = 1e-05,
  nonlinear_upper = 0.99999,
  maxit = 100,
  factr = 1e+07,
  optim_trace = 0,
  optim_report = 1,
  require_native = FALSE,
  use_gradient = TRUE
)
}
\arguments{
\item{alpha_recency_start}{Starting value for the recency learning rate.}

\item{alpha_logsuc_start}{Starting value for the logsuc learning rate.}

\item{alpha_decay_start}{Legacy starting value for the recency decay
learning rate.}

\item{online_mode}{Online optimizer mode. \code{"alpha_only"} uses the
generalized adaptive alpha layer. \code{"six_parameter"} and \code{"decay_alpha"}
retain the named historical model behaviors.}

\item{beta_alpha_terms}{Coefficient names whose beta values are updated
online.}

\item{nonlinear_alpha_terms}{Nonlinear parameter keys formatted as
\code{feature|component|slot}.}

\item{alpha_start}{Optional numeric starting vector for selected beta and
nonlinear learning rates.}

\item{alpha_lower}{Scalar or vector lower bounds for learning-rate
optimization.}

\item{alpha_upper}{Scalar or vector upper bounds for learning-rate
optimization.}

\item{nonlinear_lower}{Lower clamp for online nonlinear parameter states.}

\item{nonlinear_upper}{Upper clamp for online nonlinear parameter states.}

\item{maxit}{Maximum optimizer iterations.}

\item{factr}{Optimizer \code{factr} control value.}

\item{optim_trace}{Optimizer trace level passed to \code{stats::optim()}.}

\item{optim_report}{Optimizer reporting interval passed to \code{stats::optim()}.}

\item{require_native}{Logical; stop if the compiled native evaluator is not
loaded.}

\item{use_gradient}{Logical; pass the propagated gradient to the optimizer.}
}
\value{
A model adapter suitable for \code{LKT(model = ...)}.
}
\description{
Creates an LKT model adapter that first fits the standard
LibLinear model, then applies an online parameter-adaptation layer.
}
